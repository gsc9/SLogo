package backend.command.queries;

import backend.command.Command;

public class ShowingQuery extends Command {
	
	@Override
	public double execute() {
		//return Turtle.isShowing() ? 1 : 0;
		
		//for now:
		return 1;
	}
}
