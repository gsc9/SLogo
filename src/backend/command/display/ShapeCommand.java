package backend.command.display;

import backend.command.Command;

public class ShapeCommand extends Command {
	public double execute(){
		//return (call to controller to get shape index corresponding number)
		
		//return myController.getShapeIndex(myTurtleID);
		return 0.0;
	}
}
